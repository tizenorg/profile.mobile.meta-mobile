#!/bin/sh
echo "#################### mobile-adaptation-tm1.post ####################"

# remove exported dzImage and dzImage-recovery
rm /boot/kernel/dzImage
rm /boot/kernel/dzImage-recovery

# remove headers and docs
rm -rf /usr/include
rm -rf /usr/share/man
rm -rf /usr/share/doc
